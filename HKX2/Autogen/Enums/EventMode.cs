namespace HKX2
{
    public enum EventMode : sbyte
    {
        EVENT_MODE_DEFAULT = 0,
        EVENT_MODE_PROCESS_ALL = 1,
        EVENT_MODE_IGNORE_FROGENERATOR = 2,
        EVENT_MODE_IGNORE_TO_GENERATOR = 3,
    }
}

